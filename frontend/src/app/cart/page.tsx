import React from "react";
import { CartForm } from "@/components/CartForm";

const page = () => {
  return (
    <>
      <section>
        <CartForm />
      </section>
    </>
  );
};

export default page;
